# Mission Control
MC_API_URL=https://fletcher-mission-control-live.vercel.app
MC_API_TOKEN=                 # Mission Control bearer token

# Telegram Bot (for link drops)
TELEGRAM_BOT_TOKEN=           # From BotFather — dedicated content-drop bot, NOT the agent bots
TELEGRAM_ALLOWED_USER_ID=     # Alex's Telegram user ID — reject all other senders

# YouTube
YOUTUBE_API_KEY=              # YouTube Data API v3 key
YOUTUBE_CHANNEL_ID=           # YouTube channel ID (e.g. UCxxxxxxxx)

# Instagram
INSTAGRAM_ACCESS_TOKEN=       # Instagram Graph API long-lived token
INSTAGRAM_USER_ID=            # Instagram Business/Creator account ID

# TikTok
TIKTOK_ACCESS_TOKEN=          # TikTok API access token (if using official API)
TIKTOK_USERNAME=              # TikTok username (without @) for Apify fallback

# Optional fallbacks
APIFY_API_TOKEN=              # For TikTok/Instagram scraping if official APIs are limited
